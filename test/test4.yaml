service:
  flush: 1
  logLevel: debug

#INCLUDES:
  #@include: another file

pipeline:
  outputs:
    - name: http
      sharedKey: your-shared-key
      tls.verify: false
      tls.keyFile: your.key_file
      match: '*'
      tls.crtFile: your.crt_file
      emptySharedKey: empty shared key
      selfHostname: localhost
      tls.caFile: you.ca_file

